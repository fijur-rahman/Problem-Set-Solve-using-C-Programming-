#include <stdio.h>
#include <math.h>

int main()
{
    float a;
    scanf("%f",&a);
    printf("Sine = %.2f\n", sin(a));
    printf("Cosine = %.2f\n", cos(a));
    printf("Tangent = %.2f\n", tan(a));
    return 0;
}
